syntax = "proto3";

service AssetService {
    rpc add_asset(Asset) returns (Asset) {}
}

message Asset {
    bytes file = 1; // file of the asset
    string filename = 2;
    string type = 3;
    string metadata = 4;
}
